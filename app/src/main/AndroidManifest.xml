<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sergi.clothesapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/clothesappicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/clothesappicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.ManActivity"
            android:label="@string/MenActivity" />
        <activity
            android:name=".Activities.WomanActivity"
            android:label="@string/WomenActivity"></activity>
        <activity
            android:name=".Activities.SignUpActivity"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>